curl --location 'http://127.0.0.1:3030/generate_queries' \
--header 'Content-Type: application/json' \
--data '{
    "escrow_pubkey": "{{escrow_pubkey}}",  # String: The public key of the escrow account, Base58 encoded
    "user_private_key": "{{user_private_key}}"  # String: The private key of the user, Base58 encoded
}'